mixin accordion(data)
	.accordion&attributes(attributes)
		block

mixin accordion-item(data)
	.accordion__item(
		data-accordion-item!=`${data.open || false}`
	)&attributes(attributes)
		button(type="button" data-accordion-trigger).accordion__trigger= data.title
		.accordion__pannel.content-wrapper
			block
